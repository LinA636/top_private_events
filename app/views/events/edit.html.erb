<h1>Edit Event</h1>

<%= form_with(model: @event, local: true, method: :patch) do |f| %>
   <%= render 'form', f: f %>
    <%= button_to 'Delete Event', @event, 
        method: :delete, 
        data: { confirm: 'Are you sure?' }, 
        class: 'btn btn-danger' %>
<% end %>